// Batch B1 — secrets & credential-exposure breadth (#71, spec §B1). Entries added in the B1
// build commit; empty here so the modularization refactor is behavior-preserving on its own.

import type { CorpusEntry } from "./types.js";

export const secretsEntries: CorpusEntry[] = [];
