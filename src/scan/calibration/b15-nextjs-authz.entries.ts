// Batch B15 (#123, issues #131-#136) — Next.js/Supabase authz-shape classes seeded for the
// semantic/whole-program tier (roadmap `docs/design/corpus-roadmap-to-100.md` §4a). Six of the
// tiered rows are `review`; P-MW-SOLE-AUTHZ is the one `none` row (per-row rulings below).
// Originally NONE had a matching mechanical rule; the #354 re-triage graduated TWO
// to a leftover-auth grep (still review, not free-count): P-MW-MATCHER-EXCLUDES-API (the matcher's
// api-lookahead is a textual fact) and P-DRAFTMODE-NO-SECRET (a shallow intra-file "enable with no
// secret gate" check). #433 graduated a THIRD: P-BOLA-BODY-OWNER is now caught by the bola-owner
// AST pass (src/scan/bola-owner.ts, runs in runMechanicalScan) — session-bound handler +
// service-rooted `.eq(<ownership col>, <request-rooted value>)` + no session-vs-client comparison
// is a dataflow fact, not identity reasoning.
//
// #1366 (2026-07-31) RULED ON THE REMAINING THREE INDIVIDUALLY. The sentence that used to sit here
// was one reason covering three items — it rested on table-sensitivity judgment, cross-function
// taint, and a server→client boundary it declared off-limits to a grep/AST — and it was wrong about
// at least one of them. Each row now carries its own verdict and its own measurement:
//   P-HOST-HEADER-URL     GRADUATED. The claim "needs taint-from-Host-into-URL reasoning" was false:
//                         the read and the URL authority are in the same expression, in the same
//                         file. leftover-auth's `host-header-url` check catches it at review tier,
//                         and the row is `mustCatch` so a regression is FATAL, not a tracked gap.
//   P-MW-SOLE-AUTHZ       MEASURED GAP (`expectedTier: "none"`, gapKind "measured-gap") — outstanding
//                         work, tracked on #1679. Not a boundary.
//   P-CLIENT-RENDER-AUTHZ SUPERSEDED BY #1811 (2026-08-01), which graduated it to
//                         `expectedTier: "review"` on detectClientRenderOnlyAuthz
//                         (src/detectors/app-router.ts). #1366's measured-gap ruling and the #1011
//                         re-test it asked for are retained on the row itself, as the record #1811
//                         closes rather than as a live claim.
// #1366 is CLOSED; #1679 still tracks P-MW-SOLE-AUTHZ (its plant, its measured reason, and what a
// detector would have to see), because a comment on a closed issue is not a tracker.
// The one `none` row leaves the recall denominator and gains the fail-loud-on-graduation guard,
// which review tier does not have: a rule STARTING to fire on a review-tier row is not a failure in
// either direction, so nothing would have noticed the gap closing. (The other half of that
// sentence — that a review-tier MISS was absorbed as a tracked non-fatal gap — stopped being true
// on 2026-07-31, #1628.)
//
// This header claimed TWO `none` rows for eleven hours after #1811 graduated one of them, which is
// #1827 and why src/entries-header-drift.ts now derives these counts from the rows below instead of
// trusting the prose above them.
//
// The acceptance bar for THIS batch is precision, not recall: zero free-count (high-tier) false
// positives on the negatives. See GROUND-TRUTH.md §B15.

import type { CorpusEntry } from "./types.js";

export const b15NextjsAuthzEntries: CorpusEntry[] = [
  // #131 — object/function-level authz gap (BOLA/BFLA)
  { id: "P-BOLA-BODY-OWNER", kind: "positive", cls: "object/function-level authz gap: route trusts body.tenantId for the query (BOLA/BFLA)", location: "pages/api/billing/invoice.js", match: ["bola-body-owner"], expectedTier: "review", note: "#131/#433: pages/api/billing/invoice.js authenticates the caller but scopes the invoices query to req.body.tenantId — a client-supplied value — instead of the tenant id on the verified session. GRADUATED to a mechanical rule (#433): the bola-owner AST pass (src/scan/bola-owner.ts, in runMechanicalScan) proves the session binding, the service-rooted chain, the request-rooted .eq value, and the absence of a session-vs-client comparison — dataflow facts, not identity reasoning. Review, never free-count: the AST can't see a wrapper's gate. Discriminator vs the sibling: the .eq value's ROOT (req vs session); session-precedence keeps `const session = await getServerSession(req)` server-derived." },
  { id: "N-BOLA-SESSION-OWNER", kind: "negative", cls: "invoice query scoped to the session's tenant id, not a client-supplied field", location: "pages/api/billing/invoice-safe.js", match: ["bola-body-owner"], note: "#131/#433: pages/api/billing/invoice-safe.js scopes the query to session.user.tenantId — req.body.tenantId is never read. The #433 bola-owner pass stays fully silent here (measured, not trivial since #433): session is bound FROM req, so this pins the session-precedence rule that keeps a session-derived value out of the client set." },

  // #1267 — the SAME class with the query moved one module out. bola-owner.ts is single-file by
  // construction ("a pages/api file whose DEFAULT-EXPORTED handler is analyzable in this file"), so
  // a codebase with a data-access layer had the whole class dark. The three rows below are the
  // cross-file chain, its origin control and its far-end control; non-overlap with P-BOLA-BODY-OWNER
  // is structural (one needs the .eq() in the handler file, the other in an imported module), so no
  // call site can produce both.
  { id: "P-BOLA-CROSS-FILE", kind: "positive", cls: "BOLA where the unscoped query lives in an imported repository, not the handler", location: "pages/api/cross-file-orders.js", match: ["bola-cross-file"], expectedTier: "review", expectedSeverity: "High", note: "#1267: pages/api/cross-file-orders.js authenticates the caller and hands req.body.tenantId to listOrdersForTenant() in lib/order-repo.ts, whose parameter reaches `.eq(\"tenant_id\", tenantId)` on a service-role chain with no session comparison in either function. Caught by src/scan/bola-cross-file.ts, which resolves the real import (resolveImport) rather than guessing by callee name — the same construction ssrf-wrapper.ts uses for the SSRF sink. THE RECORD IT CORRECTS: PR #1186 filed this class under 'mechanically-impossible classes → the paid upsell'. What #915 measured is that SEMGREP OSS does not follow taint out of a function; Harvey's own AST layer already resolved imports at the time (#380), and docs/free-tier-scope.md is corrected in the same PR." },
  { id: "N-BOLA-CROSS-FILE-SESSION", kind: "negative", cls: "The same repository call, with the scoping id taken from the verified session", location: "pages/api/cross-file-orders-session.js", note: "#1267: the ORIGIN control. Identical route, identical repository function, identical far-end `.eq(\"tenant_id\", …)` — the only difference is that the id comes from session.user.tenantId. What makes the positive a bug is where the value came from, and a rule firing here would be reporting every data-access layer in existence." },
  { id: "N-BOLA-CROSS-FILE-CHECKED", kind: "negative", cls: "Request-supplied id reaches the repository, which compares it against the session first", location: "pages/api/cross-file-orders-checked.js", note: "#1267: the FAR-END control, and the one that cannot be cleared from the handler file. The request value DOES reach the repository — the syntactic half of the rule is fully satisfied — but listOrdersChecked() compares it against the session's tenant before querying, which IS the authorization check. MEASURED 2026-07-31: this row FIRED on the first implementation, because the callee's serverNames were only its db-bound values and not its other PARAMETERS, so the idiomatic `listOrders(tenantId, session)` guard read as absent." },

  // #132 — middleware matcher regex silently excludes /api/*
  { id: "P-MW-MATCHER-EXCLUDES-API", kind: "positive", cls: "Next.js middleware matcher regex excludes /api/*, leaving API routes unprotected", location: "middleware.ts", match: ["mw-matcher-excludes-api"], expectedTier: "review", note: "#132/#354: middleware.ts's config.matcher (`/((?!api|_next/static|_next/image|favicon.ico).*)`) excludes every /api/* path from ever running the middleware, so pages/api/admin/dashboard.js is reachable with zero gate. GRADUATED to a mechanical rule (#354): leftover-auth's mw-matcher-excludes-api grep matches an exported config.matcher whose negative-lookahead lists an `api` token → review. Discriminator: the api-in-lookahead contradiction with middleware presented as the gate; FP shape: a self-guarded API surface, why it's review not free-count. The negative (matcher with no api exclusion) is cleared." },
  { id: "N-MW-MATCHER-INCLUDES-API", kind: "negative", cls: "middleware matcher has no /api exclusion", location: "lib/middleware-matcher-safe.ts", match: ["mw-matcher-excludes-api"], note: "#132: lib/middleware-matcher-safe.ts's config.matcher has no 'api' exclusion — /api/* still runs through the middleware auth check. Cleared trivially (no mechanical rule); recorded to complete the corpus pair." },

  // #133 — middleware is the sole authz layer, no defense in depth
  { id: "P-MW-SOLE-AUTHZ", kind: "positive", cls: "middleware is the sole authz layer — no defense-in-depth re-check in the route/data layer", location: "pages/api/admin/dashboard.js", match: ["mw-sole-authz"], expectedTier: "none", gapKind: "measured-gap", neighbourTaxonomies: ["src.scan.rules.semgrep.harvey-select-star-pii", "Unauthenticated debug/admin route"], note: "#133/#1366: pages/api/admin/dashboard.js reads admin_metrics with no session/role check of its own, relying entirely on middleware.ts. It's a SELECT (not insert/update/delete), so the existing harvey-route-noauth mutation rule does not fire here — intentional, keeps this class isolated. RE-RULED 2026-07-31 (#1366) from a `review` recall miss to a MEASURED GAP: planted, scanned, nothing of the class fired (`pnpm exec tsx src/cli/validate-calibration.ts`, FAIL row). Outstanding work, NOT a boundary, tracked on #1679 — the discriminating signal is cross-file (does a middleware.ts exist, and does its matcher actually cover this route), and the intra-file approximation everyone reaches for first — a service-role read in a route with no session hint — was measured against this target the same day: `git grep -l 'supabaseAdmin\\|admin\\.from(' targets/calibration` returns 40 files, so that approximation has a candidate surface two orders of magnitude larger than the plant and needs a discriminator nobody has built yet. Not attempted this round; that is a statement about the afternoon, not about the world. GRADUATION GUARD, re-scoped by #1677: the `match` key used to be the whole guard and had to stay narrow, because AUTH-sensitive-route (`Unauthenticated debug/admin route`) and harvey-select-star-pii ALREADY fire on this file for other classes — an unkeyed `none` row here reads those pre-existing findings as a graduation on day one. Both are now DECLARED in neighbourTaxonomies (MEASURED 2026-07-31 by scoreGraduationGuards over a live runMechanicalScan), so the guard is every finding on the fixture minus those two: a new rule graduating onto this class fires the row whatever wording it uses, and either declared neighbour going silent fails the row as a stale claim." },
  { id: "N-MW-DEFENSE-IN-DEPTH", kind: "negative", cls: "route re-checks session/role even though middleware also covers it", location: "pages/api/admin/dashboard-safe.js", match: ["mw-sole-authz"], note: "#133: pages/api/admin/dashboard-safe.js calls getServerSession() and checks the role before returning admin_metrics, in addition to whatever middleware.ts does. Cleared trivially (no mechanical rule); recorded to complete the corpus pair." },

  // #134 — draftMode().enable() reachable with no secret
  { id: "P-DRAFTMODE-NO-SECRET", kind: "positive", cls: "draftMode().enable() reachable with no secret/token check", location: "pages/api/preview/enable.js", match: ["draftmode-no-secret"], expectedTier: "review", note: "#134/#354: pages/api/preview/enable.js calls draftMode().enable() unconditionally — any caller can flip on preview mode. The entry's old \"needs control-flow reasoning\" note overstated it: the fixture is seven lines and the enable() is the first statement (the #264 mislabel pattern). GRADUATED to a mechanical rule (#354): leftover-auth's draftmode-no-secret grep matches a draftMode().enable() with no secret/token comparison (process.env.*SECRET / req.*.secret) anywhere in the file → review. Shallow intra-file: can't see a wrapper's gate, so review not free-count. The negative (secret checked before enable) is cleared." },
  { id: "N-DRAFTMODE-SECRET-CHECKED", kind: "negative", cls: "draftMode().enable() gated by a secret check", location: "pages/api/preview/enable-safe.js", match: ["draftmode-no-secret"], note: "#134: pages/api/preview/enable-safe.js validates req.query.secret against process.env.PREVIEW_SECRET before draftMode().enable() runs. Cleared trivially (no mechanical rule); recorded to complete the corpus pair." },

  // #135 — Host header trusted to build an absolute URL (reset-link poisoning)
  { id: "P-HOST-HEADER-URL", kind: "positive", cls: "Host header trusted to build an absolute URL (reset-link poisoning)", location: "lib/reset-link.js", match: ["host-header-url"], expectedTier: "review", mustCatch: true, note: "#135/#1366: lib/reset-link.js builds the password-reset link from headers().get('host') — an attacker-controlled value — so a spoofed Host redirects the real reset token to the attacker's domain. GRADUATED 2026-07-31 (#1366). The recorded blocker — 'needs taint-from-Host-into-URL reasoning … no mechanical rule' — was ASSUMED and false: the header read and the URL authority sit in the same expression in the same file, which is a local textual fact, not cross-function taint. leftover-auth's `host-header-url` check (src/scan/leftover-auth.ts, B14_CHECKS) requires the header value to reach the AUTHORITY position of an absolute URL (`//${host}` or `'https://' + host`), which is what discriminates this class from #984's header-into-exec taint on the same read (pages/api/header-exec.js stays silent, unit-tested). Review tier, not free-count: a grep cannot see a trusted-proxy allowlist or a framework host check applied before this file runs. `mustCatch` (#1248) makes a miss FATAL — this is no longer a recall aspiration, it is a rule with a negative twin (N-HOST-HEADER-FIXED-ORIGIN) and a soundness claim. It is also the row `validate-source-recall` reported as its single recall gap." },
  { id: "N-HOST-HEADER-FIXED-ORIGIN", kind: "negative", cls: "reset link built from a fixed allowlisted origin, not the Host header", location: "lib/reset-link-safe.js", match: ["host-header-url"], note: "#135: lib/reset-link-safe.js builds the link from process.env.NEXT_PUBLIC_SITE_URL — headers()/Host is never read. Cleared trivially (no mechanical rule); recorded to complete the corpus pair." },

  // #136 — authz enforced only by a client-component conditional render
  { id: "P-CLIENT-RENDER-AUTHZ", kind: "positive", cls: "authz enforced only by a client-component conditional render; privileged data already shipped", module: "M9", location: "app/admin/page.tsx", match: ["Authorization enforced only by a client-side conditional render"], expectedTier: "review", expectedSeverity: "High", note: "#1679: GRADUATED 2026-08-01 from `expectedTier: \"none\"` (measured-gap) to a real mechanical rule, by the route #1366 and #1679 both named — `detectClientRenderOnlyAuthz` in src/detectors/app-router.ts, extending the pass that already resolves the server->client boundary. It carries `module: \"M9\"` for the same reason m9-authz.entries.ts does: the detector runs in the static-detect AST pass, not runMechanicalScan, so validate-calibration must not score it in the M1 mechanical gate. Scored live by `#1679 client-render-only authz (live detectAppRouterFindings over targets/calibration)` in calibration.test.ts. THE DISCRIMINATOR, measured against the FP surface and not only against the plant: the class needs all three of (1) an un-projected query result crossing into an imported 'use client' component, (2) NO auth call in the server component's body or in any helper #1263's resolver reaches, and (3) the client child gating only at render on one of its own props. Condition (2) alone is the whole point — the existing `M9 — Server->client data leak` check fires on BOTH page.tsx and page-safe.tsx (MEASURED 2026-08-01), so it separates neither, and it is the `getServerSession()` role check in front of the query that this rule reads. Corpus-wide FP surface MEASURED the same day over all 17 pinned commits: ZERO rows on every target, so the rule adds no noise anywhere it has been run. Review tier, never free-count, the standard M9 boundary posture: the pass proves the data crosses and that no gate is visible in what it can read, not that authorization is absent from a layout or middleware it cannot see. PRIOR, retained as the record this closes: #136/#1366: app/admin/page.tsx fetches the full admin_metrics dataset unconditionally and passes it to AdminDashboardClient.jsx, whose only gate is `if (!isAdmin) return null` — the data is already in the RSC payload regardless. Distinct from P-SERVER-CLIENT-LEAK (harvey-server-client-leak requires an exact `.select('*').eq(...)` + `{...$DATA}` spread shape; this fixture uses neither an .eq() chain nor a spread, so that rule stays silent). RE-TESTED 2026-07-31 against #1011's client-side-authz detector, which #1366 asked for by name because two sibling rows (P-CLIENT-AUTHZ-STORAGE, P-CLIENT-AUTHZ-USER) pass at review tier: `isClientSideAuthz` (src/scan/leftover-auth.ts) needs EITHER a Web Storage role read OR a `.role`-vs-privilege-literal comparison, and this fixture's gate is a plain boolean prop (`if (!isAdmin)`) with neither — so the siblings' pass says nothing about this row, and the blocker did NOT dissolve on contact. RULED a MEASURED GAP, not a boundary: the discriminating signal — a server component whose privileged query is ungated and whose result crosses into a `\"use client\"` child gated only at render — is a two-file structural fact, and the pass that already resolves that boundary is the M9 server-client-leak AST detector (src/detectors/app-router.ts). Extending it there was not attempted this round because that file was being edited by a concurrent batch; that is scheduling, not reach. Tracked on #1679." },
  { id: "N-SERVER-ROLE-CHECK", kind: "negative", cls: "role checked server-side before the privileged query/fetch runs", module: "M9", location: "app/admin/page-safe.tsx", match: ["Authorization enforced only by a client-side conditional render"], reviewTierHits: ["M9 — Server→client data leak"], note: "#1679: now a LIVE boundary, not a trivially-cleared pair member — it is the positive minus its one distinguishing line, so it is what makes the graduation above a discriminator rather than a shape match. RECORDED review-tier hit: `M9 — Server->client data leak` fires on this file too, and that is a PRE-EXISTING false positive of the sibling check (the row IS projected to the client, but the server gate means a non-admin never reaches the query) — declared here per #1344 so it cannot mask a future widening of the client-render rule onto this fixture. PRIOR: #136: app/admin/page-safe.tsx checks getServerSession()'s role and redirects BEFORE querying admin_metrics — a non-admin never causes the query to run and nothing reaches the client. Cleared trivially (no mechanical rule); recorded to complete the corpus pair." },
];
