import { createEnv } from "@t3-oss/env-nextjs";
import { z } from "zod";

// Env schema module: the single source of truth for the app's environment surface.
export const env = createEnv({
  server: {
    DATABASE_URL: z.string().url(),
    STRIPE_SECRET_KEY: z.string().min(1),
    // Declared here but never read anywhere in product source → Info (dead declaration).
    UNUSED_LEGACY_TOKEN: z.string().optional(),
  },
  client: {
    NEXT_PUBLIC_APP_URL: z.string().url(),
  },
  runtimeEnv: {
    DATABASE_URL: process.env.DATABASE_URL,
    STRIPE_SECRET_KEY: process.env.STRIPE_SECRET_KEY,
    UNUSED_LEGACY_TOKEN: process.env.UNUSED_LEGACY_TOKEN,
    NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,
  },
});
