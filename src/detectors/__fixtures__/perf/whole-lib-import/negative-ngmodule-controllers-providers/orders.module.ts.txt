// #1666 negative control: identical shape to the Nest positive fixture's module file (same
// `controllers`/`providers` array, same real controller matching REQUEST_ENTRY_PATH), but the
// decorator is `NgModule`, not `Module` — the identifier `nestModuleDecoratorArgs` actually
// checks. Angular's real decorator carries `declarations`, not `controllers`; the keys are
// spoofed in here on purpose so this control isolates the DECORATOR NAME, not the property names.
import { NgModule } from "@angular/core";
import { OrdersController } from "./controllers/orders.controller";
import { OrdersService } from "./services/orders.service";

@NgModule({
  controllers: [OrdersController],
  providers: [OrdersService],
})
export class OrdersModule {}
