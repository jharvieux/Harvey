"use client";
// `?url` yields the ASSET URL string; the worker is emitted as its own file and nothing from
// pdfjs-dist enters this chunk.
import workerSrc from "pdfjs-dist/build/pdf.worker.min.mjs?url";
import rawTemplate from "./template.html?raw";

export const config = { workerSrc, rawTemplate };
