// The SAME by-hand env parse, but no schema-validation library is in this tree — there parsing
// JSON out of an env var by hand is the ordinary approach, so the gate stays shut.
export const flags = JSON.parse(process.env.FEATURE_FLAGS || "{}") as Record<string, boolean>;
