// The SAME by-hand coercions, but no schema-validation library is in this tree — there coercing
// env values by hand is the ordinary approach, so the gate stays shut.
export const debug = import.meta.env.VITE_DEBUG === "true";
export const port = Number(import.meta.env.VITE_PORT);
export const cfg = JSON.parse(import.meta.env.VITE_CONFIG);
