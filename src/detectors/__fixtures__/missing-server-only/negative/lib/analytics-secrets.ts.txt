const apiKey = process.env.ANALYTICS_API_KEY;

export function trackEvent(name: string) {
  return fetch("https://analytics.example.com/track", { headers: { Authorization: apiKey ?? "" }, body: name });
}
