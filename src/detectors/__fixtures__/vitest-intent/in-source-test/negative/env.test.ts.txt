import { describe, expect, it } from "vitest";
import { buildUrl } from "./env.js";

describe("buildUrl", () => {
  it("joins the base and path", () => {
    expect(buildUrl("/x")).toContain("/x");
  });
});
