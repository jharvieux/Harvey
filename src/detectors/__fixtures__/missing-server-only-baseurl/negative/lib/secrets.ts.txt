// Deliberately close to, but not a candidate for, the declared `lib-secrets` package import.
// A resolver that manufactures an edge to this file turns the benign control into a finding.
export const key = process.env.STRIPE_SECRET_KEY;
