# PLANTED BUG (P-PUBLIC-DIR-SENSITIVE): an env file committed under public/ — Next.js serves it at
# /.env.production, handing config (and any secrets in it) to anyone. Caught by
# checkPublicDirSensitive → high. Fake, inert values below.
NEXT_PUBLIC_APP_NAME=calibration-fixture
API_BASE_URL=https://api.example.com
