{
  "name": "b10-vuln-deps-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "INERT calibration fixture (#71 B10). A standalone project root pinning KNOWN-VULNERABLE versions of common Next.js/Supabase-stack packages, each below a curated CVE fix boundary in src/scan/dependencies.ts. next@16.0.5 is on the Server Actions null-origin CSRF line (and, correctly, the RSC/WS-SSRF lines too); next-auth@4.24.5 is the email-misdelivery version (above the OAuth-CSRF fix, so it isolates that class). All versions are declared as DATA ONLY — NEVER installed. See GROUND-TRUTH.md §B10.",
  "dependencies": {
    "next": "16.0.5",
    "next-auth": "4.24.5",
    "jsonwebtoken": "8.5.1",
    "follow-redirects": "1.15.4",
    "axios": "1.7.2",
    "undici": "5.7.0",
    "cookie": "0.5.0",
    "ws": "7.4.5",
    "sharp": "0.31.3"
  }
}
