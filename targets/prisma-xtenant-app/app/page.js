// The fixture's tenancy surface is the team API routes; this root page just lets `next build` render
// a landing route (#1163).
export default function Home() {
  return <main>prisma-xtenant-app — intentionally vulnerable M2 fixture. Never deploy.</main>;
}
