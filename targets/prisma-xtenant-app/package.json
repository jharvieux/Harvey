{
  "name": "prisma-xtenant-app",
  "version": "0.0.0",
  "private": true,
  "description": "INTENTIONALLY VULNERABLE M2 cross-tenant fixture for the Harvey pen-test kit (#796). Never deploy. A minimal Prisma membership app (User <- TeamMember -> Team) with one properly tenant-scoped team route and one unscoped route, so the authenticated cross-tenant BOLA probe's positive+negative branches are regression-gated offline. Answer key: README.md.",
  "scripts": {
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
