// Single source for the venture's contact address, so a prospect who hits an error on the
// scan intake path (the site's only conversion path) always has somewhere to go.
export const SUPPORT_EMAIL = "info@harvey-qa.com";
