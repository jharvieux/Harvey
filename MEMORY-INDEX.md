# MEMORY index

Standing context and recent decisions from `MEMORY.md`, newest first. Read this file
at session start. Search `MEMORY-INDEX-ARCHIVE.md`, then the matching full entry in
`MEMORY.md`, only when the current task touches older context; never load either file
in full by default.

Every `## D-NNN` entry in `MEMORY.md` appears in exactly one index. New decisions enter
this lean index. Move shipped, superseded, or no-longer-active one-liners to the archive
as an exact move, keeping this startup view bounded.

## Entries

- D-001 — 2026-09-07 — Add project-owned durable decision memory
