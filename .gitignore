# Vitals provenance data
.vitals/

node_modules/
coverage/
dist/
report-template/out/
.epic-builder/
*.log
.DS_Store

# M8 (#72) Stryker output — targets/calibration/test-quality/ (generated, machine/run-specific;
# the answer-key capture lives in src/mutation-scan.test.ts instead)
reports/mutation/
stryker-tmp/

# Transient sweep/agent worktrees — nested checkouts of this repo, never committed
.claude/worktrees/

# Dry-run timing — non-deterministic per-run timestamps, not committed (#302, #842)
dry-run/timing.json
