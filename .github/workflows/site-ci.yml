# #1592: nothing in CI ran the site's OWN build or lint. `site-smoke.yml` only probes the
# DEPLOYED site over HTTP, so a page that fails to build never reaches it — a broken page never
# deploys, and site-smoke stays green against the last good deployment while `main` carries the
# break. This job runs `site/`'s own toolchain instead: `npm run build` (which typechecks via the
# Next TS plugin AND performs the static-generation pass — see below) and `npm run lint`
# (`next lint`, now backed by a real `eslint-config-next` config). CORRECTED 2026-07-30: an earlier
# draft of this comment claimed pre-PR `next lint` exited 0 unconditionally ("No ESLint warnings or
# errors") — that is FALSE. MEASURED 2026-07-30 from a clean `npm ci` at `main@9001e7a`
# (no eslint dependency present, no eslint config file): `npm run lint` prints Next's "How would
# you like to configure ESLint?" interactive setup prompt, then exits 1 because CI has no TTY to
# answer it (reproduced via `CI=true npm run lint < /dev/null`, both in a plain shell and matching
# GitHub Actions' non-interactive stdin) — it never reached any actual lint check, so the gap was a
# loud, unconditional exit-1 failure, not a silent pass. `site-smoke.yml`/nothing invoked
# `npm run lint` before this PR either way, so the broken command's exit code never blocked
# anything — the real defect this PR closes.
#
# `site/` is a SEPARATE, self-contained npm project — its own `package.json` under npm, not a
# pnpm workspace member (`pnpm-workspace.yaml` declares no packages). A pnpm invocation inside it
# is a category error; this job uses `npm ci` / `npm run …` throughout, never `pnpm`.
#
# Why no separate `tsc --noEmit` step: MEASURED 2026-07-30 — a page that throws at render time
# (`throw new Error(...)` at the top of a page component, which is syntactically and
# type-correctly valid TypeScript) passes `tsc --noEmit` with exit 0 but fails `next build` with
# exit 1 during the "Generating static pages" step, because `next build` actually performs the
# static-generation pass `tsc` never runs. `next build`'s own "Linting and checking validity of
# types" step already re-uses the Next TS plugin and fails loud on a real type error (also
# MEASURED, same session: exit 1 on an injected `number`/`string` mismatch). So `next build` is a
# strict superset of `tsc --noEmit` here — it typechecks AND catches the render-time class `tsc`
# structurally cannot see — and a bare `tsc` step would add CI time without adding coverage.
#
# NOT a required status check (operator call — #1592's acceptance says recommend, don't
# self-grant). Because of that this uses a plain `paths:` filter on the trigger rather than the
# in-job two-tier filter conservation.yml/corpus-drift.yml use for THEIR required contexts: #1107's
# deadlock (a path-filtered REQUIRED check never reports on a PR outside its filter, so branch
# protection waits forever) only applies once a check is required. If this is later promoted to
# required, the filter must move in-job with a green no-op short-circuit — copy the shape
# conservation.yml uses for its `relevant` flag — before it is registered, exactly as #1107 forced
# for dry-run-drift.

name: site-ci

on:
  pull_request:
    paths:
      - "site/**"
      - ".github/workflows/site-ci.yml"
  push:
    branches: [main]
    paths:
      - "site/**"
      - ".github/workflows/site-ci.yml"
  workflow_dispatch:

concurrency:
  group: site-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: site build + lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: site
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: site/package-lock.json

      # npm, not pnpm: site/ is its own npm project (own package-lock.json), not a pnpm
      # workspace member. `pnpm-workspace.yaml` at the repo root declares no packages.
      - run: npm ci

      # Typechecks (via the Next TS plugin) AND performs the static-generation pass — see the
      # header note on why no separate `tsc` step is added.
      - run: npm run build

      # `next lint`, backed by `site/eslint.config.mjs` (flat config, `next/core-web-vitals` via
      # `FlatCompat`). See the header note: without an eslint dependency present this exited 1 on
      # an unanswerable interactive setup prompt and never ran a real lint check.
      - run: npm run lint
