---
name: run-audit
description: How to execute a Harvey engagement — the ten-module (M1–M10) run table with the exact command per module and its tier/prereq, the source/free vs connected vs dynamic vs paid-LLM tier split, and the run modes of the single `run-audit` orchestrator (free / validation / paid, re-audit diff, SARIF + SBOM exports). Use whenever running, scoping, or scripting an audit against a target.
---

# Running a Harvey audit

Scope authority: `briefs/audit-modules.md`. All ten modules carry **equal weight** — an ad-hoc
(e.g. security-only) subset is a defect, not a scan. The coverage-guard doctrine that governs a
module which cannot run lives in the repo root `AGENTS.md` and always applies.

### The ten modules and how to run each

| # | Module | Run it with | Tier / prereq |
|---|--------|-------------|---------------|
| M1 | Multi-tenant security | `pnpm quick-scan --dir <t>` (mechanical) + `pnpm exec tsx src/cli/scan.ts --supabase <ref\|local>` (config) + LLM `/vuln-scan --extra briefs/scan-extras.txt --extra <pnpm scan-focus M3-hotspots.txt>` → `/triage --fp-rules briefs/fp-rules.txt` (semantic; the M3 hotspot focus prioritizes review on the hottest files, #442) + `pnpm detect-deeper` (live RLS/policy review) | free → connected → paid-LLM |
| M2 | Local pen-test (dynamic) | `pnpm exec tsx src/cli/pentest.ts` against a local `supabase start` stack seeded with two tenants. Every probed run emits an `M2-SCOPE-RECONSTRUCTION` disclosure (#875) — what was stood up from which schema at which commit, that production config was NOT observed — plus a per-surface/per-identity ledger where an unexercised access path is a `not probed` row with its reason. Probe families: PostgREST + pg_graphql cross-tenant matrices (#877), session-lifecycle/auth-attack (#658/#878), invitation-state revoked-BOLA (#905), soft-delete/restore/tenant-teardown data-residue (#907/#954), a share-link identity-class probe (#952), a Supabase Storage object-authorization probe (#956), an API-token / service-account credential-store probe (#997 — the token store's readability by a non-owner; the app-mediated token-AUTHENTICATION half stays disclosed not-assessed), a guest / cross-org-collaborator probe (#1023 — the live stand-up seeds a THIRD auth user holding one membership row with a schema-resolved third role value, signs it in, and proves whether that partially-privileged session reaches another tenant; a schema with no third legal role is a disclosed not-applicable, never a silent two-persona run, and whether the guest's reach INSIDE its own tenant is correct stays an app-role-matrix question reported as measured row counts), and a control-gated Realtime subscribe-and-assert runtime probe opt-in behind `HARVEY_PROBE_REALTIME` (#951, superseding the #906 NOT-ASSESSED disclosure; Phoenix wire shape proven live against realtime v2.100.0 — both the leak and the tenant-scoped verdict — #1003, `docs/design/realtime-wire-shape-live-proof.md`; a verdict PER published table, not just the first, #1030; Broadcast + Presence now probed too — cross-identity reach on one topic with their own own-changes controls, plus the private-channel/Realtime-Authorization posture, wire shape measured 2026-07-25 against realtime v2.100.0, #1029); PUT-only routes are discovered (#908) | needs live stack; `assertComplete` gates target coverage |
| M3 | Hotspot analysis | `pnpm exec tsx src/cli/hotspot-scan.ts <t>` — wraps `vitals_cli.py report --json` (pass `--report <capture>` when vitals is off PATH, #314; `--hotspots-out` feeds M8's `--hotspots` and M6 `simplify-scan --hotspots`, #442; the installed vitals is asserted to report the pinned `EXPECTED_VITALS_VERSION`, failing loud on drift, #808; when vitals is entirely unavailable a Harvey-side git-only churn×complexity REDUCED TIER runs and is disclosed `partial`, #807) | external plugin; run, don't build |
| M4 | Duplication | `pnpm check:duplication` or `pnpm quality-scan <t>` (jscpd + diverged-clone near-miss pass, #360) | source-only |
| M5 | Slop / dead code | `pnpm quality-scan <t>` (knip dead code) + `pnpm detect-static <t>` (slop AST detectors — narrating comments, orphan TODOs, try/catch-rethrow, single-call wrappers/single-use helpers, placeholder stubs, unused params/imports, unreachable branches, … `src/detectors/slop.ts`) — running only one engine is half the module | both tiers source-only; if the target's deps aren't installed and knip can't load its config, M5 re-runs with all knip plugins disabled and reports **review-tier** dead code, disclosed as an **M5-98** row (#810) — install the target's deps for confirmed (non-review) file findings |
| M6 | Simplification / maintainability | `pnpm quick-scan --dir <t>` (free report: non-grading "looks hand-rolled" indicators, rolled up per class per file, #267) + `pnpm detect-static <t>` (same indicators as raw findings) + `pnpm simplify-scan <t>` (assembles the `briefs/quality-extras.txt` brief + source into a review packet) | free = non-grading "looks hand-rolled" indicators; paid = triage + name the replacement (#267). **`simplify-scan` produces a review PACKET, not a verdict — printing source is not running M6; the verdict is a human/LLM pass over the packet (#351).** **First run against a real target** (stoimera/Cravab, 2026-07-18) — the paid verdict is recorded via the M6 pass artifact + `run-audit --record`, so M6 is no longer in `MODULES_NEVER_EXECUTED` (#283, see `docs/design/m6-first-real-target-verdict.md`) |
| M7 | Performance | `pnpm detect-static <t>` (code tier) + `pnpm quick-scan --dir <t>` (static RLS initplan, #374) + `pnpm perf-scan` (DB advisors) + `pnpm lighthouse-scan` (Core Web Vitals / Lighthouse, local build+serve+browser, #387) | source (code) + connected (advisors) + local-run (Lighthouse) |
| M8 | Test quality | `pnpm detect-static <t>` (test-intent detectors, #372) + `pnpm mutation-scan <t> --stub-check` (pre-Stryker deletion survival, #373) + `pnpm mutation-scan <t>` (StrykerJS) | source (test-intent; stub-check needs the target's tests runnable but no Stryker install); full mutation run needs the target test suite; a missing Stryker config is scaffolded per detected runner (vitest/jest/mocha), and missing Stryker packages install `--no-save` only with `--install` (#513); a scoped run records `partial` with its scope (#504); a failed dry run surfaces as a distinct env-fragile-suite verdict (#503); **no tests → emit a zero-coverage finding, don't skip** (#224) |
| M9 | Server→client boundary/rendering | `pnpm detect-static <t>` (M9 AST) — no longer Next-only: since #916/#917/#918 a framework-agnostic boundary model routes Remix, React Router 7 and TanStack Start to adapters (server→client leak + server-action authz/validation etc.); Astro/SvelteKit/Nuxt stay disclosed not-assessed pending the SFC parse layer (#920/#921). A non-Next target the model doesn't cover gets a not-assessed row naming the framework (#872), never a silent Next-premise run | source-only |
| M10 | Data classification (PII/PHI/PCI) | `pnpm pii-classify` — `SUPABASE_DB_URL=…` (live) or `--schema <path…>` over a migrations dir, a `.sql` file, discovered root/nested schema DDL, or a `schema.prisma` (#250/#529/#770/#758) | connected or schema |

Source/free tier = M1(mechanical), M4, M5, M6(indicators, #267), M7(code), M8(test-intent, #372), M9, M10(schema); connected adds M1(live), M7(advisors), M10(live); dynamic = M2; paid-LLM = M1(semantic), M6(triage), M3-depth. Per-module free/paid split: `docs/free-tier-scope.md` + the `product-shape-decisions` memory.

**Run modes are ONE orchestrator (`pnpm exec tsx src/cli/run-audit.ts <t>`, aliased as `pnpm run-audit <t>`) gated by env flags, not three tools.** Every mode runs all ten modules; a tier not in scope records `requires-live-run`/`partial` with a reason, never a skip (the flags declare which environments the engagement HAS, not which modules to run).
- **Free** = `run-audit <t>` — source, mechanical only.
- **Validation** = `run-audit <t> --llm --dynamic --artifacts-dir <dir>` — source + the LLM passes over source + a local-stack pen-test, but NO client DB. "Validation" is running everything against a public repo we were given nothing on; dynamic (M2) still applies because we stand up our OWN local Supabase (`pnpm dynamic-validate <repo>` assesses stand-up-ability, `--execute` runs it). The orchestrator does not itself run the LLM or the pen-test — those are operator/skill passes that leave a `<module>.pass.json` (`pnpm record-pass …` / `pnpm dynamic-validate`), which `--artifacts-dir` reads to derive `ran`.
- **Paid** = add `--connected` (a live read-only client DB) for the connected tiers (M1 live, M7 advisors, M10 live).
- **Re-audit diff:** `run-audit <t> --findings-out cur.json --baseline <prior findings.json>` classifies each finding resolved / persistent / new against a prior engagement of the same client (stable taxonomy+location identity), for repeat customers (#457).
- **Machine-readable exports (#867/#887):** `run-audit <t> --sarif-out <file>` emits SARIF 2.1.0 for GitHub code-scanning / ASPM ingestion — the per-module coverage ledger travels with it as `toolExecutionNotifications`, so a module that did not run cannot vanish into "no results"; `--sbom-out <file>` emits a CycloneDX 1.5 dependency inventory. `--sarif-out` is a standalone deliverable — it turns findings capture on by itself and exports the same finding set `--findings-out` would (#1061; before that fix it silently exported only what survived on stdout). Both flags are also on `quick-scan`. Dependency-CVE findings carry an import/call reachability signal for ranking, still excluded from grading (#874).